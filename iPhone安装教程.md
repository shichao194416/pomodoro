# 番茄钟 · iPhone 安装教程

从零到在 iPhone 桌面上用上这个番茄钟，全程大约 15 分钟。不需要 Mac，不需要 Apple 开发者账号，不需要花钱。

---

## ⚡ 已经部署好了，直接跳到第五节

**你的 App 地址：<https://shichao194416.github.io/pomodoro/>**

线上已经跑起来了，全部资源校验通过。**现在你只需要做两件事**：

1. 用 iPhone 的 **Safari** 打开上面这个地址
2. 分享 → **添加到主屏幕**

具体操作见 **第五节**。第三节是已经完成的部署记录（含以后怎么更新），第四节是备用的 Netlify 方案，第一次安装可以先跳过。

---

## 零、先说清楚原理

iPhone 装 App 有三条路：

| 方式 | 需要 | 结论 |
|---|---|---|
| App Store / TestFlight | Mac + 99 美元/年开发者账号 | ❌ 不适合 |
| 数据线侧载（AltStore） | 一台常开的电脑 + 每 7 天重签 | ❌ 太折腾 |
| **网页 App（PWA）加到主屏幕** | 只要一个 HTTPS 网址 | ✅ **本教程走的路线** |

第三条路的效果：桌面上有独立图标、点开全屏无地址栏、能离线用、有启动画面，用起来和原生 App 基本没区别。代价是 iOS 的一些系统级限制（见第七节，**必读**）。

流程分两大步：

1. **把代码发布到一个 HTTPS 网址上**（第五节的方案 A 或 B 二选一）
2. **在 iPhone 上用 Safari 打开那个网址，添加到主屏幕**

---

## 一、准备工作

- 电脑上已经装好 Node.js（本项目在 Node 24 上开发和验证）
- 一个能上网的浏览器
- iPhone

先确认本地能构建成功。打开 PowerShell：

```powershell
cd C:\Users\15547\Desktop\番茄钟\pomodoro-app
npm install
npm run build
```

最后看到 `✓ built in ...` 和 `precache 18 entries` 就说明没问题。

> 如果 npm 报 EPERM 之类的错误，加一句：`npm install --ignore-scripts`
> 这条命令跳过安装脚本；本项目依赖的平台二进制走 optional 依赖，不影响使用。

---

## 二、先在本机看一眼效果（可选，1 分钟）

```powershell
cd C:\Users\15547\Desktop\番茄钟\pomodoro-app
npm run serve
```

然后浏览器打开：

- 正常尺寸：<http://127.0.0.1:4173/>
- **iPhone 尺寸对照（横屏 + 竖屏）**：<http://127.0.0.1:4173/__dev/frame.html>

第二个页面会把 App 嵌在 844×390（iPhone 横屏）和 390×844（竖屏）两个框里，可以直接确认横屏布局对不对。

想让手机也能访问（同一个 Wi-Fi）：

```powershell
npm run serve -- 4173 0.0.0.0
```

命令会打印出形如 `http://192.168.x.x:4173/` 的地址，手机浏览器输入即可。
**注意**：局域网是 http 不是 https，离线缓存不会生效，只适合先看看效果，正式使用请继续往下走。

---

## 三、方案 A：发布到 GitHub Pages ✅ 已完成

> **这一步已经帮你做完了，不用再动手。**
>
> - 线上地址：<https://shichao194416.github.io/pomodoro/>
> - 源码仓库：<https://github.com/shichao194416/pomodoro>
> - 发布方式：`main` 分支存源码，`gh-pages` 分支存构建产物
>
> 下面 3.1–3.4 保留原始步骤作为参考；**3.5 记录了这次实际踩到的三个坑，3.6 是以后更新线上版本的方法**。

### 3.1 建一个空仓库

1. 打开 <https://github.com/new>
2. **Repository name** 填 `pomodoro`（名字随便，但记住它，它决定最终网址）
3. 可见性选 **Public**
4. **不要**勾选 "Add a README file"
5. 点 **Create repository**

### 3.2 把代码推上去

在 PowerShell 里执行（把 `<你的用户名>` 换成你的 GitHub 用户名）：

```powershell
cd C:\Users\15547\Desktop\番茄钟\pomodoro-app

git init
git add .
git commit -m "番茄钟：横屏布局 + 每日总结 + 到点提醒"
git branch -M main
git remote add origin https://github.com/<你的用户名>/pomodoro.git
git push -u origin main
```

首次 push 会弹出登录窗口，选「用浏览器登录」授权即可。

> **如果提示 `Please tell me who you are`**，先补上身份信息再重新 commit：
> ```powershell
> git config --global user.name "你的名字"
> git config --global user.email "你的邮箱"
> git commit -m "番茄钟：横屏布局 + 每日总结 + 到点提醒"
> ```

### 3.3 开启 Pages

1. 进入仓库页面 → **Settings**（设置）
2. 左侧栏点 **Pages**
3. **Build and deployment → Source** 选 **GitHub Actions**（⚠️ 不要选 "Deploy from a branch"）
4. 不用点保存，选完即生效

### 3.4 等它自动构建

1. 点仓库顶部的 **Actions** 标签
2. 会看到一条名为 **Deploy 番茄钟 to GitHub Pages** 的任务在跑
3. 等它变成**绿色对勾**（第一次大约 1–2 分钟）
4. 你的网址就是：

```
https://<你的用户名>.github.io/pomodoro/
```

> 如果 Actions 是红色叉：点进去看报错。绝大多数情况是没把 `pomodoro-app` 里的文件推到仓库根目录。仓库首页应该能直接看到 `package.json`、`src/`、`index.html`。

### 3.5 这次实际踩到的三个坑

**坑 1：Token 缺 `workflow` 权限，推不了 Actions 工作流**

推送 `.github/workflows/` 下的文件时被 GitHub 拒绝：

```
refusing to allow an OAuth App to create or update workflow
`.github/workflows/deploy.yml` without `workflow` scope
```

解决办法：工作流文件挪到 `deploy/github-actions-deploy.yml` 备用，改用 **`gh-pages` 分支**发布（只需要普通的 `repo` 权限）。

**坑 2：`github.com:443` 被网络阻断，`git push` 连不上**

诊断结果（同一时刻）：

| 目标 | 443 端口 |
|---|---|
| `github.com` | ❌ 超时 |
| `api.github.com` | ✅ 通 |
| `ssh.github.com` | ✅ 通 |
| `shichao194416.github.io` | ✅ 通 |

DNS 解析出的 IP 是正常的，所以是 TCP 层被阻断——国内访问 GitHub 的常见情况。随时可以自己测：

```powershell
cd C:\Users\15547\Desktop\番茄钟\pomodoro-app
npm run netcheck
```

绕过办法：**用 GitHub REST API 推送**，全程只访问 `api.github.com`。

**坑 3：空仓库不能直接建 blob**

新建的空仓库用 Git Data API 建文件会报 `409 Git Repository is empty.`，必须先打一个初始提交。这个已经在 `tools/gh-api-push.mjs` 里自动处理。

### 3.6 以后怎么更新线上版本

改完代码后，一条命令：

```powershell
cd C:\Users\15547\Desktop\番茄钟\pomodoro-app
npm run deploy
```

它会自动：按 `/pomodoro/` 基路径构建 → 把 `dist/` 通过 API 推到 `gh-pages` 分支。推完约 1 分钟生效。

然后校验线上资源：

```powershell
npm run verify
```

输出里全部是 `ok 200` 就说明发布成功。

> 哪天网络恢复（或你开了代理），`git push` 也能用。但注意：**`git push` 只更新源码，不会更新线上站点**，之后仍要跑一次 `npm run deploy`。

---

## 四、方案 B：Netlify Drop（完全不想碰命令行）

1. 先在电脑上构建：

   ```powershell
   cd C:\Users\15547\Desktop\番茄钟\pomodoro-app
   npm run build
   ```

2. 浏览器打开 <https://app.netlify.com/drop>
3. 把 `C:\Users\15547\Desktop\番茄钟\pomodoro-app\dist` **整个文件夹拖进网页**
4. 等十几秒，得到一个形如 `https://随机名字.netlify.app` 的 HTTPS 地址
5. （建议）注册个免费 Netlify 账号并认领这个站点，否则临时站点过一段时间会被回收

这个地址就是你的 App 网址，效果和方案 A 一样。缺点是没有自动更新——改完代码要重新拖一次。

---

## 五、在 iPhone 上添加到主屏幕 ⭐ 关键步骤

**必须用 Safari。** 微信内置浏览器、Chrome、Edge 都不行（Chrome 只能加个书签，做不出独立全屏 App）。

1. 用 **Safari** 打开 <https://shichao194416.github.io/pomodoro/>
2. 等页面**完全加载出来**（看到番茄图标和 `25:00`）
3. 点屏幕**底部中间的「分享」按钮**（一个方框 + 向上箭头）
4. 在弹出的菜单里**向下滑动**，找到 **「添加到主屏幕」**，点它
5. 名称会自动填成「番茄钟」，点右上角 **「添加」**
6. 回到桌面 —— 一个番茄图标出现了

点开它：应该是**全屏、没有地址栏、没有 Safari 界面**的独立 App。

> **图标不对？** 如果桌面图标显示成网页截图或问号：
> 长按删除它，回到 Safari，**确认页面完全加载完**再执行「添加到主屏幕」。
> 另外必须用 Safari，用微信打开是加不出正确图标的。

---

## 六、首次使用设置

1. 点右上角 **☰** 打开设置
   - **学习时长**：默认 25 分钟，改成你想要的值（1–60）
   - **休息时长**：默认 5 分钟（1–30）
   - **提示音**：保持开启 ⚠️
   - **语言**：默认已经是「简体中文」
   - 点 **保存**
2. 回到主界面，点中间的 **▶ 播放按钮**开始学习
   - ⚠️ **第一次点播放同时会解锁铃声**。这是 iOS 的硬性要求：网页必须由用户点一下才能出声。所以第一次一定要真点一下播放，别用别的方式启动。
3. 计时期间，倒计时下方会显示 **「☀ 屏幕将保持常亮」**，表示防止息屏已生效
4. 时间到 → 全屏弹出提醒卡片 + **连响 3 声** + 震动
   - 学习结束 → 按钮是 **「开始休息」**
   - 休息结束 → 按钮是 **「继续学习」**
   - 这个提醒**不会自动消失**，就是为了保证你能看到
5. 点底部 **「总结」** 标签看每日总结：
   - **今日学习**：今天累计学了多久（大字）
   - **今日番茄**：今天完成了几次
   - **连续天数 / 学习天数 / 总时长**
   - **每日记录**：每天一行的历史列表 + 柱状条
   - 下面还有「按标签」的细分统计和历史明细

---

## 七、横屏怎么用

**手机横过来就自动进入「专注模式」**。整个屏幕只有四样东西：

```
                                   ┌──────────────┐
                                   │ ⤡ 退出横屏    │
                                   └──────────────┘
                ┌───────────────────────┐
                │      专注学习          │
                │    ╭───────────╮      │
                │   │   24:37    │      │  ← 发光进度环，随倒计时消耗
                │    ╰───────────╯      │
                │        ( ▶ )          │  ← 开始 / 暂停
                └───────────────────────┘
```

界面元素只有：**倒计时（带进度环）、开始/暂停、当前阶段标签、右上角「退出横屏」**。
顶栏、底栏、标签选择、备注、统计全部**不渲染**，所以画面是干净的。

其他要知道的：

- **进度环**会随剩余时间消耗，专注是红色、休息是青色，背景光晕也跟着换色。
- **「退出横屏」按钮**点下去就回到完整界面（双栏：左边倒计时，右边标签和备注）。注意：网页 App 没法让手机真的转回竖屏——iOS 不提供这个接口——所以这里是「退出沉浸模式」的意思。想看总结或改设置就点它。
- 退出后顶栏会出现一个 **⤢ 专注模式** 按钮，可以随时回到全屏。
- 把手机转回竖屏、再横过来，会自动重新进入专注模式。
- 在「总结」标签页横屏**不会**进入专注模式，方便你横着看统计。
- 短屏机型（比如 iPhone SE 横屏只有 320px 高）会自动缩小环和字号。

---

## 八、iOS 的限制（必读，避免以为坏了）

### 1. 锁屏 / 切到别的 App 后，到点不会弹系统通知

iOS **不允许网页 App 在后台运行或发通知**。这是系统的硬限制，所有网页版番茄钟都一样。

本 App 的对策是：

- 计时期间**保持屏幕常亮**。只要你把手机放在桌上、屏幕亮着、App 在前台，到点一定会响。
- 如果你手动锁屏或切走了，**回到 App 的瞬间会立刻补响提醒并弹出卡片**。剩余时间是按真实时间戳算的，**不会算错**（比如 25 分钟过去了 30 分钟才回来，它会显示已结束，而不是还剩 20 分钟）。

**所以建议**：学习时把手机立起来、屏幕朝上放着，别锁屏。

### 2. 静音拨杆会静音提示音

iPhone 侧面那个静音开关打开时，网页音频会被静音。**学习时请把它关掉（拨到不显示橙色的一侧）。**

### 3. 数据存在本机，清掉就没了

记录保存在 Safari 的 `localStorage` 里：

- 设置 → Safari → 清除历史记录与网站数据 → **记录会丢**
- 删除主屏幕图标 → **记录会丢**
- 手机长期不打开这个 App，iOS 也可能回收数据

**建议**：重要的阶段结束时，在「总结」页截个图存档。

### 4. 音量靠媒体音量

响铃时用的是媒体音量。如果媒体音量被调到最低，声音会很小。响铃前把音量调上去。

---

## 九、常见问题

**Q：到点了没声音？**
依次检查：
1. 侧面**静音拨杆**是否关掉
2. 手机**媒体音量**是否够大
3. 设置里**提示音**是否开着
4. 这一次计时开始前，是否点了 **▶ 播放按钮**（第一次点播放才会解锁音频）

**Q：桌面图标是白底问号 / 网页截图？**
删掉图标，用 **Safari** 重新打开网址，**等页面完全加载**后再「添加到主屏幕」。不要用微信或 Chrome 添加。

**Q：打开是 Safari 界面，不是全屏 App？**
说明是当书签打开的，不是从主屏幕图标打开的。要从**桌面那个番茄图标**点进去。

**Q：改了代码推送后，手机上还是旧版？**
App 有自动更新机制。依次试：
1. 从桌面图标打开 App，等几秒，它会在后台下载新版本
2. 完全关掉 App（上滑关闭）再重新打开
3. 还不行：删除主屏幕图标 → Safari 重新打开网址 → 重新添加到主屏幕

**Q：断网还能用吗？**
第一次用 Safari 打开之后，Service Worker 会把全部资源（包括提示音）缓存下来，之后断网也能正常计时和响铃。但**必须先用 Safari 完整打开过一次**。

**Q：想换学习时长，一定要开设置吗？**
不用。直接**点倒计时数字**（计时中不能点），可以就地修改这一轮的时间。

**Q：换手机 / 换浏览器数据会同步吗？**
不会。数据只在本机。本 App 完全本地运行，没有账号、没有服务器、不上传任何数据。

---

## 十、日常更新与维护

改完代码后：

```powershell
cd C:\Users\15547\Desktop\番茄钟\pomodoro-app
npm test          # 跑冒烟测试
npm run deploy    # 构建 + 发布到线上
npm run verify    # 校验线上资源全部可访问
```

`npm run deploy` 会自动完成构建和发布，约 1 分钟后手机上就能拿到新版本。
如果想同时把源码同步到 GitHub，可以再 `git add . && git commit -m "..." && git push`（网络允许时）。

手机上刷新到新版本的方法见第九节。

想重新画图标（改颜色、改形状就编辑 `tools/make-icons.mjs`）：

```powershell
npm run icons
```

---

## 附：命令速查

| 目的 | 命令 |
|---|---|
| 本地开发（热更新） | `npm run dev` |
| 构建生产版本 | `npm run build` |
| 本地预览构建产物 | `npm run serve` |
| 局域网给手机看 | `npm run serve -- 4173 0.0.0.0` |
| iPhone 尺寸对照页 | <http://127.0.0.1:4173/__dev/frame.html> |
| 跑测试 | `npm test` |
| **发布到线上** | **`npm run deploy`** |
| **校验线上站点** | **`npm run verify`** |
| 诊断 GitHub 连通性 | `npm run netcheck` |
| 重新生成图标 | `npm run icons` |
| 依赖装不上时 | `npm install --ignore-scripts` |
