/**
 * Personal settings.
 *
 * The greeting is deliberately NOT part of the translations table: it is a
 * personal message rather than a UI string, and it should read the same in
 * every language. Change the text here and it updates everywhere.
 */
export const HEADER_GREETING = '加油李世超';
